spring:
  servlet:
    encoding:
      charset: UTF-8
      force: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/friendkurly?serverTimezone=Asia/Seoul
    username: bi
    password: 1234

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug

kurly:
  savedCsv:
    path: C:\Java\kurly-backend\src\main\resources\csv\5users_recommendations.csv